// ---------------------------------------------------------------------------
// Punch clock configuration
// ---------------------------------------------------------------------------
// Fill in the two values below, save the file, and upload it next to
// index.html. Both come from your Supabase project:
//
//   Supabase dashboard -> your project -> Project Settings (gear icon)
//   -> "API" (or "Data API") page:
//
//   SUPABASE_URL      = the "Project URL", e.g. https://abcdefghijkl.supabase.co
//   SUPABASE_ANON_KEY = the "anon" / "public" key (a long string starting "eyJ",
//                       or a "publishable" key starting "sb_publishable_").
//
// The anon key is designed to be public: it identifies the project and nothing
// more, because every rule is enforced inside the database (schema.sql).
//
// NEVER put the "service_role" key (or any "secret" key) in this file or
// anywhere else in the site. That key bypasses every security rule, and
// anything in a website can be read by anyone who opens it.
// ---------------------------------------------------------------------------
window.APP_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
